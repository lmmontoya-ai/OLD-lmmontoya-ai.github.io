import React from "react";
import { Callout } from "./Callout";

// Add more custom MDX components as needed
export const MDXComponents = {
  Callout,
  // You can map other MDX elements to custom React components here
};
