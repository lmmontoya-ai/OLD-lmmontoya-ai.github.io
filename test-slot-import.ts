// Temporary test file to verify @radix-ui/react-slot import
import { Slot } from '@radix-ui/react-slot';

// Basic type check - if this compiles, the package is properly installed
const testSlot: typeof Slot = Slot;

export { testSlot };
